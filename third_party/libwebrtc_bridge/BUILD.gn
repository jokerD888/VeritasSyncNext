# The target is declared by root_build.patch because only a root target is
# permitted to link //:webrtc. This marker file keeps the bridge source
# directory self-describing when copied into a pinned checkout.
